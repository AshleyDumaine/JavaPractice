import com.sun.prism.paint.Color;

public class PaintFill {
public void fill(Object pixel, Color new_color) {
	// for (Object neighbor : pixel.neighbors()) {
	//    if (neighbor.color() == pixel.color()) {
	//       neighbor.fill();
	//       neighbor.setColor(new_color);
	//    }
	// }
}
}
